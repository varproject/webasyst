<?php

class valenaBackendLayout extends waLayout
{
    public function execute()
    {
        // dd(waRequest::post());
        // dd(waRequest::param());

        // $app = $this->getApp();
        // $user = $this->getUser();
        // $admin = $user->getRights($app, 'backend');
        // $can_edit = $this->getRights();
        // $user_id = $this->getUserId();
        // $contact_id = wa()->getUser()->getId();


        // dd($projects);



        // $group_model  = new apanelGroupModel();
        // $groups = $group_model->getAll();

        // $project_model  = new apanelProjectModel();
        // $projects = $project_model->getByField('user_id', $this->getUserId(), true);

        // // dd($groups);

        // $this->view->assign([
        //     'groups'    => $groups,
        //     'projects'  => $projects,
        //     'uris'      => URIS,
        // ]);
    }
}
