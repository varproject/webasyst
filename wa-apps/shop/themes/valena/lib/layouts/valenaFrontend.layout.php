<?php

class valenaFrontendLayout extends waLayout
{
    public function execute()
    {
        // dd(waRequest::post());
        // dd(waRequest::param());

        // $app = $this->getApp();
        // $user = $this->getUser();
        // $admin = $user->getRights($app, 'backend');
        // $can_edit = $this->getRights();
        // $user_id = $this->getUserId();
        // $contact_id = wa()->getUser()->getId();


        // dd($projects);
    }
}
